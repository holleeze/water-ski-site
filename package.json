{
  "type": "module",
  "dependencies": {
    "node-fetch": "^3.3.2",
    "cheerio": "^1.2.0"
  },
  "scripts": {
    "generate": "node ./scripts/generate_article_data.js",
    "generate:gallery": "node ./scripts/generate_gallery_data.js",
    "generate:all": "npm run generate && npm run generate:gallery"
  }
}
